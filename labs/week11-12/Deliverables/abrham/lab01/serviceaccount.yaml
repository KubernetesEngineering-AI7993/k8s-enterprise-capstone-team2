apiVersion: v1
kind: ServiceAccount
metadata:
  name: readonly-app-sa
  namespace: dev
  labels:
    app: readonly-app
    env: dev
  annotations:
    description: "Least-privilege ServiceAccount for read-only pod inspection in dev"
automountServiceAccountToken: false   
