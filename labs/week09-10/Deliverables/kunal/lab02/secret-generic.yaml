# =============================================================================
# AdGuard Home – Generic Secret
# =============================================================================
# IMPORTANT: The values below are base64-encoded placeholders for learning
# purposes only. Never commit real credentials to Git.
#
# To generate your own base64 values:
#   echo -n 'your-value' | base64
#
# Equivalent kubectl command:
#   kubectl create secret generic adguard-credentials \
#     --from-literal=admin-username=admin \
#     --from-literal=admin-password=changeme123 \
#     --from-literal=upstream-dns-token=fake-dns-token-abc123 \
#     -n adguard-home
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: adguard-credentials
  namespace: adguard-home
  labels:
    app: adguard-home
    lab: week09-10-lab02
type: Opaque
data:
  # echo -n 'admin' | base64
  admin-username: YWRtaW4=
  # echo -n 'changeme123' | base64
  admin-password: Y2hhbmdlbWUxMjM=
  # echo -n 'fake-dns-token-abc123' | base64
  upstream-dns-token: ZmFrZS1kbnMtdG9rZW4tYWJjMTIz
