apiVersion: v1
kind: Pod
metadata:
  name: toleration-demo
spec:
  nodeSelector:
    dedicated: ai7993
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "ops"
    effect: "NoSchedule"
  containers:
  - name: app
    image: nginx

