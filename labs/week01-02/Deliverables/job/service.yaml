apiVersion: v1
kind: Service
metadata:
  name: web-server-service
  namespace: staging   
spec:
  type: ClusterIP       
  selector:
    app: nginx          
  ports:
    - protocol: TCP
      port: 8080         
      targetPort: 80    